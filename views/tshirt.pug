extends layout

block content
  h1= title
  p Welcome to #{title}
  - var results = [{color:"Black", brand: "Tommy Hilfiger", price: 20},{color:"Red", brand: "Calvin Kelvin", price: 18},{color:"White", brand: "Buffallo", price: 22}]
   div(class="container-fluid")
    div(class="row" )
     each res,index in results
      div(class="col-sm-4" onclick=`buy(${index})`) Color : #{res.color} , Brand : #{res.brand} , Price : #{res.price} 
  script.
   let itemNumber=0                
        function buy(itemNumber)
        {
            //for index in results
             alert(`you purchased Item ${itemNumber+1}`)
        }